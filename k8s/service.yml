apiVersion: v1
kind: Service
metadata:
  name: moscow-time-service
spec:
  selector:
    app: moscow-time
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
